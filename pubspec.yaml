name: deeplink
description: Flutter SDK for the Deeplink deep-linking service.
version: 0.1.0
homepage: https://github.com/YOURORG/deeplink-sdk-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
